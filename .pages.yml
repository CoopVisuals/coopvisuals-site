media:
  input: images
  output: /images

content:
  - name: pages
    label: Pages
    type: collection
    path: .
    match: "{index,services,about,contact}.html"
    fields:
      - name: title
        label: Page Title
        type: string
      - name: body
        label: Content
        type: rich-text